// ── Named provider identifiers ──────────────────────────────────────────────
export const PROVIDER_OPCODE = 'opencode';
export const PROVIDER_PUTER = 'puter';
export const PROVIDER_CHATGPT = 'chatgpt';

// ── Provider collections ────────────────────────────────────────────────────
export const DEFAULT_PROVIDERS = [PROVIDER_OPCODE, PROVIDER_PUTER, PROVIDER_CHATGPT];
export const DEFAULT_ORDER_PROVIDERS = [PROVIDER_OPCODE, PROVIDER_PUTER, PROVIDER_CHATGPT];
export const PROVIDER_NAMES = [PROVIDER_OPCODE, PROVIDER_PUTER, PROVIDER_CHATGPT];
export const DEFAULT_PROVIDER = PROVIDER_OPCODE;
